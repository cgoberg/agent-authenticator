# Changelog

## 0.2.0

- hardened vault writes to avoid concurrent corruption
- added a real CLI with `serve`, `doctor`, and `keygen`
- removed secret leakage from first-run key handling
- cleaned up HTTP defaults to prefer loopback safety
- upgraded the repository for public Forge Nord OSS launch
